#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{

    char texte[20];
    printf("entrer une texte:");
    gets(texte);
    printf("le texte saisi est:");
    puts(texte);
    return 0;
}
